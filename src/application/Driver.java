package application;

import java.sql.*;

public class Driver {
	
	public static void main(String[] args){
		
		/*PreparedStatement myStatement = null;
		ResultSet myRs = null;
		String h8 = "h8m8?";
		try{
			
			// Get a DB connection
			Connection myConn = DriverManager.getConnection("jdbc:mysql://localhost:3306/cinema_ticketsystem_db", "root", "volimnebo6!r");
			//Create a statement
			myStatement = myConn.prepareStatement("INSERT INTO movie (`title`, `director`, `description`) VALUES (?,?,?)");

			//Execute SQL query
			myStatement.setString(1, h8);
			myStatement.setString(2, "Ayyy");
			myStatement.setString(3, "Byyy");
			
			myStatement.executeUpdate();
		}
		catch(Exception exc){
			exc.printStackTrace();
		}
	*/	
	}
	}